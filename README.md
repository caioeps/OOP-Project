# POO
Projeto para disciplina de POO

Sistema de gerenciamento de funcionários

A fazer:
	Anotações;
	Classes:
		Finanças;
		Saude(subs dentro de variavel);
		Legal;
		Gerente;
		Marketing(em geral);

Fazendo:
	Classes:
		Empregado(abstrata);
		Engenheiro(subs dentro de variavel);
		Tecnico(subs dentro de variavel);

	Interface gráfica:
		Janela cheia, lista dos empregados, menu superior para operações;

Feito:
